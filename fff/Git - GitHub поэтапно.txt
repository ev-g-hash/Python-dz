(git config --global user.name "ev-g-hash")
(git config --global user.email "pihtulovevgeny@gmail.com")


git config --global user.name "E-pihta"
git config --global user.email "eo.plehanov79@gmail.com"


git init

git add . (если нужно файл или папку то засунь в кавычки)

git commit -m "упражнения поколение Пайтон"

git remote add https://github.com/E-pihta/prob.git

git push -u origin master

если делать всё поэтапно то всё работает, есть нюансы... через практику осваиваются!!!!!!!!!!!


